package com.example.popgrid;



public class ListAdapter
//        extends BaseAdapter
{

  /*  LayoutInflater inflater;
    private List<Data> Datalist = null;
    private ArrayList<Data> arraylist;
    Context context;

    public ListAdapter(Activity context) {
        this.context=context;
        inflater = LayoutInflater.from(context);

    }

    @Override
    public int getCount() {
        return Datalist.size();
    }

    @Override
    public Object getItem(int position) {
        return Datalist.get(position);
    }

    @Override
    public long getItemId(int position) {
        return position;
    }

    public View getView(final int position,View view,ViewGroup parent) {
        View Item =inflater.inflate(R.layout.childgv, null,true);

        TextView txtTitle = (TextView) Item.findViewById(R.id.textV);

        txtTitle.setText(Datalist.get(position).getNames());

        Item.setOnClickListener(new View.OnClickListener() {

            @Override
            public void onClick(View arg0) {
                grid = inflater.inflate(R.layout.gridv, null);
                TextView textView = (TextView) grid.findViewById(R.id.textV);
                textView.setOnClickListener(activity);
            }
        });

        return Item;



    }; */
}
